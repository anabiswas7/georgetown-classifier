
import os
import sys
import pickle
import argparse

def main(*argv):

    # Create command line argument parser
    parser = argparse.ArgumentParser(description="Classify products with a prebuilt model.")

    # Add arguments to parse
    # TODO

    # Parse the arguments
    options = parser.parse_args()

    # Handle the text
    # TODO

if __name__ == '__main__':
    main(*sys.argv)
